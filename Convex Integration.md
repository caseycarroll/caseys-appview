# Convex Integration
1. Scaffold convex in project
2. Define schema for state and session tables
3. Initialize convex client as a singleton, create a convex lib module
4. Wire on session and state getters/setters to DB
5. Go through a login flow
6. validate storage using npx convex dashboard
